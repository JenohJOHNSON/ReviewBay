# Shared dependencies for both the ingestion worker and the API.
# Pinned loosely; tighten before production.

# --- ingestion ---
apify-client>=1.7            # Google Maps / Yelp / TripAdvisor / IG / FB / web actors
google-play-scraper>=1.2     # Google Play reviews (free, no key/token)
requests>=2.31              # App Store (Apple RSS), Tavily, Firecrawl, Reddit API
psycopg[binary]>=3.2        # Postgres (Neon) driver
PyYAML>=6.0
playwright>=1.44            # self-hosted Trustpilot scraper (ingestion image also runs `playwright install chromium`)
selectolax>=0.3            # fast HTML parser for the Playwright-rendered pages

# --- sentiment + insights (local, free) ---
vaderSentiment>=3.3        # local sentiment scoring (tiny lexicon, no model to load)
scikit-learn>=1.4          # TF-IDF + KMeans for the dashboard insights (no LLM)

# --- api / rag / embeddings ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
openai>=2.0                 # OpenAI Responses API (chat) + embeddings API (vectors)
