# Installed into the Airflow image on top of the base apache/airflow image.
# Providers for the SQL + (optional) Airbyte operators, plus the ingestion
# runtime deps so PythonOperator can import and run reviewbot connectors.

apache-airflow-providers-snowflake
apache-airflow-providers-common-sql
apache-airflow-providers-airbyte

# reviewbot ingestion + enrich runtime (the Airflow workers scrape, load, and
# embed; they don't serve the API)
apify-client>=1.7
snowflake-connector-python>=3.12
PyYAML>=6.0
fastembed>=0.3             # local embeddings for the enrich task
vaderSentiment>=3.3        # local sentiment for the enrich task
